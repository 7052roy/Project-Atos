using UnityEngine;
using System.Collections;

public class Scroller : MonoBehaviour {
    public float scrollspeed;
    private Vector2 savedOffset;

    // Use this for initialization
    void Start() {
        //savedOffset = renderer.shared
    }
	
	// Update is called once per frame
	void Update () {
	
	}
}
